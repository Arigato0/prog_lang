

numbers := [1, 8, 3, 32]

# sql like syntax for querying any container
even_numbers := select n from numbers where n % 2 == 0

println(even_numbers)

scores := map {
    "john smith": 623,
    "allen baker": 92,
    "richard rook": 320
}

# highest is a keyword that will find the max score
highest_score := select name, score from scores with highest score

println(highest_score)
