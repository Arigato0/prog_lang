
struct User implements IUser:
    
    fn ctor(username):
        this.username = username
    fn greet():
        println("hello user")

    fn login(username, password):
        _handle_login(username, passowrd)

result.x += 10

my_user := User("alex")

my_user.name = "john"
